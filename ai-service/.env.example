# OpenAI provider secret. Set only on the server; never commit the populated file.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-6-astra
# Separate random code for each device, comma separated. Generate with:
# node -e "process.stdout.write(require('node:crypto').randomBytes(32).toString('base64url'))"
VAELORIA_CLIENT_TOKENS=
PORT=8080
HOST=0.0.0.0
